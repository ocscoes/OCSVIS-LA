---
title: "Cohesión Social en América Latina"
format:
  dashboard: 
   nav-buttons:
     - github
     - icon: globe
       href: https://ocs-coes.netlify.app
logo: "www/images/ocs-negro.png"
server: shiny
resources: 
 - www
 - css
---

```{r}
#| context: setup


## URLs
# logo_file <- "https://github.com/ocscoes/OCS-COES/raw/master/app/www/images/ocs-21.png"
# url_ocs   <- "https://ocs-coes.netlify.app/"

## Cargar librerías
library(tidyverse)
library(sf)
library(leaflet)
library(plotly)
library(shinyjs)
library(shinyWidgets)
library(shinyEffects)
library(shinydashboardPlus)
library(shinydashboard)
library(ggrepel)
library(cowplot)
library(magick)
library(highcharter)
library(fontawesome)
library(scales)
library(ggthemes)
library(RColorBrewer)
library(hrbrthemes)
library(sjlabelled)
library(V8)
library(Cairo)
options(shiny.usecairo = TRUE)


## Cargar datos

load("data/world_data.rdata")

if (!inherits(world_data, "sf")) {
  world_data <- st_as_sf(world_data)
}


unique_years <- unique(world_data$Ola)
step_size <- unique(diff(unique_years))

# Bivariado

# load(file = "data/base_shiny.rdata")
# 
# df <- df %>% filter(País!="Bahamas" & País !="Grenada" & País != "Suriname")
# 
# unique_years_bi <- unique(df$Ola)
# step_size_bi <- unique(diff(unique_years))
# 
# # Cálculo de los rangos mínimos y máximos para cada variable
# range_values <- df %>%
#   group_by(Variable) %>%
#   summarise(min_val = min(Valor, na.rm = TRUE),
#             max_val = max(Valor, na.rm = TRUE))

# Base con jerarquías

load(file = "data/base_shiny_jerarq.rdata")

df <- df_jerarquico %>% filter(País!="Bahamas" & País !="Grenada" & País != "Suriname")

unique_years_bi <- unique(df$Ola)
step_size_bi <- unique(diff(unique_years))

# Cálculo de los rangos mínimos y máximos para cada variable
# range_values <- df %>%
#   group_by(Variable) %>%
#   summarise(min_val = min(Valor, na.rm = TRUE),
#             max_val = max(Valor, na.rm = TRUE))

```

# Inicio

1.  Elegir una de las pestañas disponibles para realizar el análisis:

  -   **_Mapa_**: En esta sección se proyectan en el mapa de América latina los promedios de cada país para cada dimensión o subdimensión. Por defecto está seleccionado para el promedio histórico, pero es posible seleccionar un año de interés.
  -   **_Longitudinal_**: En este apartado se compara a los países a través de un gráfico de líneas histórico. Debes seleccionar una dimensión o subdimensión a visualizar. Es posible seleccionar qué país o países se presentan.
  -   **_Bivariado_**: Aquí se encuentran dos selectores entre los que encontrarás las dimensiones y subdimensiones además de algunas variables de caracterización de los países tales como su PIB o esperanza de vida.

2.  Seleccionar características del gráfico:

  a.  Seleccione el indicador
  b.  Seleccione el País
  c.  Seleccione la Ola o rango de tiempo: Cada una de las Olas representa el periodo en que se realizó el trabajo de campo.
  d.  Si desea generar un nuevo gráfico, repite los pasos anteriores.
  
  
Más información sobre las fuentes de datos, la selección de indicadores y la construcción de los índices en: 

# Mapa

## {.sidebar width="20%"}

```{r}
sliderInput("Ola", "Selecciona una ola",
            min = min(world_data$Ola),
            max = max(world_data$Ola),
            step = 2,
            value = max(world_data$Ola))
```

```{r}
div(style = "
  display: flex; 
  flex-wrap: wrap; 
  gap: 6px;",
  
  lapply(1:8, function(i) {
    btn_id <- paste0("btn_var", i)
    btn_label <- c(
      "Cohesión<br>General",
      "Cohesión<br>Horizontal",
      "Cohesión<br>Vertical",
      "Confianza<br>Interpersonal",
      "Seguridad<br>Pública",
      "Confianza en<br>Instituciones",
      "Actitud hacia<br>la Democracia",
      "Justicia<br>Redistributiva"
    )[i]
    
    btn_icon <- c(
      "cohesion-general.svg",
      "cohesion-horizontal.svg",
      "cohesion-vertical.svg",
      "Confianza interpersonal.svg",
      "seguridad publica.svg",
      "Confianza en instituciones.svg",
      "Actitud hacia la democracia.svg",
      "Justicia distributiva.svg"
    )[i]
    
    div(style = "
        width: 48%;
        height: 125px;
        display: flex;
        align-items: center;
        justify-content: center;",
      actionButton(
        inputId = btn_id,
        label = HTML(glue::glue(
          '<div style="text-align:center; font-size:12px; line-height:1.1;">
             <img src="www/img/{btn_icon}" width="35"><br>{btn_label}
           </div>'
        )),
        class = "btn btn-light",
        style = "width: 100%; height: 100%; padding: 5px;"
      )
    )
  })
)

```



## map


```{r}
 leafletOutput("map", height = "1050px")
```


# Análisis Longitudinal


## {.sidebar width="20%"}

```{r}
selectInput("pais", "País(es)", choices = NULL, multiple = TRUE, selected = "Chile")
selectInput("indice", "Índice", choices = NULL)
selectInput("dimension", "Dimensión", choices = NULL)
selectInput("subdimension", "Subdimensión", choices = NULL)
selectInput("indicador", "Indicador", choices = NULL)

```


## Gráfico

```{r}
 plotlyOutput("longitudinal_plotly", height = "800px")
```


# Análisis Bivariado

## {.sidebar width="20%"}

```{r}
sliderInput("Ola_bi", "", min = min(unique_years_bi), max = max(unique_years_bi), value = max(unique_years_bi), step = 2, sep = "")

            selectInput("x_var", "Variable para el eje X:", choices = unique(df$Variable), selected = "Cohesión horizontal")
            selectInput("y_var", "Cariable para el eje Y:", choices = unique(df$Variable), selected = "Cohesión vertical")
            
            checkboxInput("show_flags", "Banderas en lugar de puntos.", TRUE)
```

## Gráfico

```{r}
plotlyOutput("plot", height = "800px")
```



```{r}
#| context: server


## MAPA -----

# Variable reactiva para la dimensión seleccionada
selected_variable <- reactiveVal("")

button_var_map <- list(
  btn_var1 = "Cohesión general",
  btn_var2 = "Cohesión horizontal",
  btn_var3 = "Cohesión vertical",
  btn_var4 = "Confianza Interpersonal",
  btn_var5 = "Seguridad Pública",
  btn_var6 = "Confianza en las Instituciones",
  btn_var7 = "Actitudes a la Democracia",
  btn_var8 = "Justicia Redistributiva"
)

# Observadores para los botones
lapply(names(button_var_map), function(btn_id) {
  observeEvent(input[[btn_id]], {
    selected_variable(button_var_map[[btn_id]])
  })
})

# Render inicial del mapa
output$map <- renderLeaflet({
  leaflet(world_data) |>
    addTiles() |>
    setView(lng = -78.4678, lat = -1.8312, zoom = 3.5)
})

# Observador para actualizar el mapa cuando cambia ola o variable
observe({
  req(input$Ola)
  req(selected_variable())

  variable <- selected_variable()

  data_year <- world_data %>% filter(Ola == input$Ola) %>% st_as_sf()
  

  # Validar que la variable exista en los datos
  if (!variable %in% names(data_year)) {
    showModal(modalDialog(
      title = "Error",
      paste0("La variable '", variable, "' no se encuentra en los datos."),
      easyClose = TRUE
    ))
    return()
  }

  # Si todos los valores son NA, no dibujar
  if (all(is.na(data_year[[variable]]))) {
    leafletProxy("map") |>
      clearShapes() |>
      clearControls() |>
      addTiles() |>
      setView(lng = -78.4678, lat = -1.8312, zoom = 3.5)

    showModal(modalDialog(
      title = "Atención",
      "No hay datos disponibles para esta combinación de año y variable. Intenta con otra ola.",
      easyClose = TRUE
    ))
    return()
  }

  # Crear paleta de colores
  bins <- c(floor(min(data_year[[variable]], na.rm = TRUE)),
            quantile(data_year[[variable]], probs = seq(0.2, 1, 0.2), na.rm = TRUE)) |>
          unique()

  if (length(bins) < 2) {
    bins <- seq(floor(min(data_year[[variable]], na.rm = TRUE)),
                ceiling(max(data_year[[variable]], na.rm = TRUE)), length.out = 5)
  }

  pal <- colorBin(palette = colorRampPalette(c("#e5f5e0", "#006d2c"))(length(bins) - 1),
                  domain = data_year[[variable]],
                  bins = bins)

  # Actualizar el mapa
  leafletProxy("map", data = data_year) |>
    clearShapes() |>
    clearControls() |>
    addTiles() |>
    addPolygons(
      stroke = FALSE,
      fillOpacity = 0.7,
      color = ~pal(data_year[[variable]]),
      label = ~paste0(País, ": ", round(data_year[[variable]], 1)),
      labelOptions = labelOptions(
        textsize = "15px",
        style = list("font-weight" = "500")
      )
    ) |>
    addLegend(pal = pal,
              values = data_year[[variable]],
              title = variable,
              position = "bottomright",
              labFormat = labelFormat(digits = 1)) |>
    setView(lng = -78.4678, lat = -1.8312, zoom = 3.5)
})

## LONGITUDINAL ----

observe({
  updateSelectInput(session, "pais", 
                    choices = unique(df$País), 
                    selected = "Chile")
})

observe({
  updateSelectInput(session, "pais", choices = unique(df$País))
  updateSelectInput(session, "indice", choices = unique(df$Indice))
})

observeEvent(input$indice, {
  dims <- df %>% filter(Indice == input$indice, Nivel == "Dimension") %>% pull(Dimension) %>% unique()
  updateSelectInput(session, "dimension", choices = dims)
})

observeEvent(input$dimension, {
  subs <- df %>% filter(Dimension == input$dimension, Nivel == "Subdimension") %>% pull(Subdimension) %>% unique()
  updateSelectInput(session, "subdimension", choices = subs)
})

observeEvent(input$subdimension, {
  inds <- df %>% filter(Subdimension == input$subdimension, Nivel == "Indicador") %>% pull(Indicador) %>% unique()
  updateSelectInput(session, "indicador", choices = inds)
})

output$longitudinal_plotly <- renderPlotly({
  req(input$pais, input$indicador)

  temp_df <- df %>%
    filter(País %in% input$pais, Nivel == "Indicador", Indicador == input$indicador)

  p <- plot_ly()

  for (pais in unique(temp_df$País)) {
    df_pais <- temp_df %>% filter(País == pais)

    p <- add_trace(
      p,
      x = df_pais$Ola,
      y = df_pais$Valor,
      type = 'scatter',
      mode = 'lines',
      name = pais,
      hovertemplate = paste("País: ", pais,
                            "<br>Ola: %{x}<br>Valor: %{y}<extra></extra>")
    )
  }

  p <- p %>% layout(
    title = paste("Serie temporal:", input$indicador),
    xaxis = list(title = "Ola", range = c(2003, max(temp_df$Ola, na.rm = TRUE))),
    yaxis = list(title = "Valor")
  )

  return(p)
})


# ## BIVARIADO ----
# 
# noDataMessage <- reactiveVal(FALSE)
#   
#   observe({
#     x_var <- input$x_var
#     y_var <- input$y_var
#     time_point <- input$Ola_bi
#     
#     temp_df <- df %>% filter(Ola == time_point)
#     
#     x_data <- temp_df %>% filter(Variable == x_var, .preserve = TRUE) %>% pull(Valor)
#     y_data <- temp_df %>% filter(Variable == y_var, .preserve = TRUE) %>% pull(Valor)
#     
#     x_available <- nrow(temp_df %>% filter(Variable == x_var, .preserve = TRUE)) > 0 && any(!is.na(x_data))
#     y_available <- nrow(temp_df %>% filter(Variable == y_var, .preserve = TRUE)) > 0 && any(!is.na(y_data))
#     
#     if (x_available && y_available) {
#       removeModal()
#       noDataMessage(FALSE)
#     } else {
#       showModal(modalDialog(
#         tags$div(tags$h4("Atención"), style = "display: inline-block;"),
#         tags$button(tags$i(class = "fa fa-times"), type = "button", class = "close", `data-dismiss` = "modal", 
#                     `aria-label` = "Close", style = "margin-left: 15px;"),
#         "No hay datos disponibles para la variable en este año. Por favor, seleccione otro.",
#         easyClose = TRUE,
#         footer = NULL
#       ))
#       noDataMessage(TRUE)
#     }
#   })
#   
#   
#   get_flag_emoji <- function(country_name) {
#     sanitized_country_name <- gsub(" ", "_", country_name)
#     
#     flag_emojis <- list(
#       Argentina          = "\U0001F1E6\U0001F1F7",
#       Belize             = "\U0001F1E7\U0001F1FF",
#       Bolivia            = "\U0001F1E7\U0001F1F4",
#       Brazil             = "\U0001F1E7\U0001F1F7",
#       Canada             = "\U0001F1E8\U0001F1E6",
#       Chile              = "\U0001F1E8\U0001F1F1",
#       Colombia           = "\U0001F1E8\U0001F1F4",
#       Costa_Rica         = "\U0001F1E8\U0001F1F7",
#       Dominican_Republic = "\U0001F1E9\U0001F1F4",
#       Ecuador            = "\U0001F1EA\U0001F1E8",
#       El_Salvador        = "\U0001F1F8\U0001F1FB",
#       Guatemala          = "\U0001F1EC\U0001F1F9",
#       Guyana             = "\U0001F1EC\U0001F1FE",
#       Haiti              = "\U0001F1ED\U0001F1F9",
#       Honduras           = "\U0001F1ED\U0001F1F3",
#       Jamaica            = "\U0001F1EF\U0001F1F2",
#       Mexico             = "\U0001F1F2\U0001F1FD",
#       Nicaragua          = "\U0001F1F3\U0001F1EE",
#       Panama             = "\U0001F1F5\U0001F1E6",
#       Paraguay           = "\U0001F1F5\U0001F1FE",
#       Peru               = "\U0001F1F5\U0001F1EA",
#       "Trinidad_&_Tobago" = "\U0001F1F9\U0001F1F9",
#       United_States      = "\U0001F1FA\U0001F1F8",
#       Uruguay            = "\U0001F1FA\U0001F1FE",
#       Venezuela          = "\U0001F1FB\U0001F1EA",
#       Promedio           = "\U0001f6a9"
#     )
#     
#     return(flag_emojis[[sanitized_country_name]])
#   }
#   
#   # Esto es para calcular los minimos y máximos globales, con el fin de fijar los ejes. 
#   global_ranges <- reactive({
#   list(
#     x = range(df %>% filter(Variable == input$x_var) %>% pull(Valor), na.rm = TRUE),
#     y = range(df %>% filter(Variable == input$y_var) %>% pull(Valor), na.rm = TRUE)
#   )
# })
# 
#   
#   output$plot <- renderPlotly({
#     if (noDataMessage()) return(NULL)
#     
#     x_var <- input$x_var
#     y_var <- input$y_var
#     time_point <- input$Ola_bi
#     show_flags <- input$show_flags  # Aquí se añade el estado del checkbox
#     
#     temp_df <- df %>% filter(Ola == time_point)
#     
#     p <- plot_ly()
#     for(pais in unique(temp_df$País)){
#       print(pais)
#       x_vals <- temp_df %>% filter(País == pais, Variable == x_var) %>% pull(Valor)
#       y_vals <- temp_df %>% filter(País == pais, Variable == y_var) %>% pull(Valor)
#       
#       if (show_flags) {
#         # Aquí se añade la funcionalidad para mostrar las banderas como emojis
#         flag_emoji <- get_flag_emoji(gsub(" ", "_", pais))
#         p <- add_text(
#           p,
#           x = x_vals,
#           y = y_vals,
#           text = flag_emoji,
#           name = pais,
#           size = 5,
#           hovertemplate = paste("País: ", pais,
#                                 "<br>", x_var, ": %{x}",
#                                 "<br>", y_var, ": %{y}<extra></extra>")
#         )
#         
#         p <- p %>% layout(
#           font = list(family = "Noto Color Emoji, Open Sans, verdana, arial, sans-serif"),
#           legend = list(font = list(family = "Noto Color Emoji, Open Sans, verdana, arial, sans-serif"))
#         )
#       } else {
#         # Aquí está tu código original para mostrar los marcadores
#         p <- add_markers(
#           p,
#           x = x_vals,
#           y = y_vals,
#           name = pais,
#           marker = list(
#             sizemode = "diameter",
#             size = 12  # Tamaño de la imagen
#           ),
#           hovertemplate = paste("País: ", pais,
#                                 "<br>", x_var, ": %{x}",
#                                 "<br>", y_var, ": %{y}<extra></extra>")
#         )
#       }
#     }
#     
#     # Adición de título y títulos de ejes
#     # p <- p %>% layout(
#     #   title = "Análisis bivariado para subdimensiones de cohesión social",
#     #   xaxis = list(title = x_var),
#     #   yaxis = list(title = y_var)
#     #   # plot_bgcolor = '#ECF0F5',  # Cambio de color de fondo
#     #   # paper_bgcolor= '#ECF0F5'  # Cambio de color de fondo
#       
#       p <- p %>% layout(
#   title = "Análisis bivariado para subdimensiones de cohesión social",
#   xaxis = list(
#     title = x_var,
#     range = global_ranges()$x
#   ),
#   yaxis = list(
#     title = y_var,
#     range = global_ranges()$y
#   )
# )
#     
#     return(p)
#   })

```


